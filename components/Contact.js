import { Box, Typography } from '@material-ui/core';

export default function Contact() {
    return (
        <Box display="flex" m={2} pt={1} pb={1}>
            <Typography variant="h6" gutterBottom>Want to chat? Email me or reach out on Twitter</Typography>
        </Box>
    );
};